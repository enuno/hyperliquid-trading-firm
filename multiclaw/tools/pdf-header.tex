% Dense layout: small margins, more text per page, page numbers
\usepackage[margin=0.6in, top=0.6in, bottom=0.75in, left=0.6in, right=0.6in]{geometry}
\usepackage{setspace}
\onehalfspacing
\pagestyle{plain}
% Ensure page numbers in footer (article class default; reinforce if needed)
\setlength{\footskip}{0.5in}
